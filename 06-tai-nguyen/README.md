# Quản lý tài nguyên

Ảnh của các bài nằm cạnh bài học trong thư mục `images/`; SVG website mẫu nằm trong thư mục `assets/` của mã nguồn tương ứng nhằm tránh hỏng đường dẫn.

Quy tắc: đặt tên `01-ten-noi-dung.png`, ghi nguồn/giấy phép cho ảnh lấy từ bên ngoài, tối ưu dung lượng. Không tải ảnh có watermark hoặc nội dung vi phạm quyền sử dụng.
